# JSONFormatter 手动测试检查清单

## 测试环境

在以下平台完成测试后,请在对应项打勾:

- [ ] Windows 10
- [ ] Windows 11
- [ ] macOS Intel (x64)
- [ ] macOS Apple Silicon (aarch64)

---

## 1. 安装测试

### Windows 安装

- [ ] MSI 安装包可正常下载
- [ ] MSI 安装包双击后启动安装向导
- [ ] MSI 安装完成后可在开始菜单找到应用
- [ ] MSI 安装的应用可正常启动
- [ ] NSIS 安装包可正常下载
- [ ] NSIS 安装包双击后启动安装向导
- [ ] NSIS 安装完成后可在开始菜单找到应用
- [ ] NSIS 安装的应用可正常启动
- [ ] Windows SmartScreen 警告可通过"更多信息"→"仍要运行"绕过
- [ ] 卸载程序可正常卸载应用

### macOS 安装

- [ ] DMG 文件可正常下载
- [ ] DMG 文件可正常挂载
- [ ] 可将应用拖拽到"应用程序"文件夹
- [ ] 首次运行时 Gatekeeper 提示可通过右键→"打开"绕过
- [ ] 应用可正常启动
- [ ] 应用可正常卸载(拖拽到废纸篓)

---

## 2. 功能测试

### 2.1 编辑器模式

- [ ] 应用启动后默认显示编辑器模式
- [ ] 可在文本区域输入 JSON 内容
- [ ] 点击"格式化"按钮后 JSON 被正确格式化并缩进
- [ ] 格式化后 JSON 语法高亮正确显示
- [ ] 点击"清空"按钮后编辑器内容被清除
- [ ] 点击"粘贴"按钮后剪贴板内容被粘贴到编辑器
- [ ] 点击图钉图标后窗口置顶,图标状态变化
- [ ] 再次点击图钉图标后取消置顶,图标状态恢复
- [ ] 点击转义按钮后 JSON 字符串被正确转义
- [ ] 再次点击转义按钮后 JSON 字符串被正确反转义
- [ ] 点击主题图标后切换到浅色主题
- [ ] 再次点击主题图标后切换回深色主题
- [ ] 点击最小化按钮后窗口最小化
- [ ] 点击关闭按钮后窗口关闭(最小化到托盘)

### 2.2 分屏模式

- [ ] 点击视图切换按钮后切换到分屏模式
- [ ] 分屏模式下左侧显示编辑区,右侧显示树形视图区
- [ ] 分屏模式下可在左侧输入 JSON 内容
- [ ] 分屏模式下点击"格式化"后右侧显示树形视图
- [ ] 分屏模式下点击"清空"后左侧编辑区和右侧树形视图均被清除
- [ ] 分屏模式下点击"粘贴"按钮后内容被粘贴到左侧编辑区
- [ ] 分屏模式下窗口置顶功能正常
- [ ] 分屏模式下转义功能正常
- [ ] 分屏模式下主题切换正常
- [ ] 分屏模式下树形视图可展开节点
- [ ] 分屏模式下树形视图可折叠节点
- [ ] 再次点击视图切换按钮后切换回编辑器模式
- [ ] 视图模式在重启后保持(localStorage 持久化)

### 2.3 TreeView 交互

- [ ] 对象节点左侧显示展开/折叠箭头
- [ ] 数组节点左侧显示展开/折叠箭头
- [ ] 点击箭头后节点展开,显示子节点
- [ ] 再次点击箭头后节点折叠,隐藏子节点
- [ ] 点击"展开全部"后所有节点展开
- [ ] 点击"折叠全部"后所有节点折叠
- [ ] 双击键名后路径被复制到剪贴板
- [ ] 双击值后路径被复制到剪贴板
- [ ] 字符串值显示为绿色
- [ ] 数字值显示为蓝色
- [ ] 布尔值显示为橙色
- [ ] null 值显示为灰色
- [ ] 对象节点显示子节点数量(如 `{3}`)
- [ ] 数组节点显示元素数量(如 `[5]`)

### 2.4 LaTeX 公式渲染

- [ ] 行内公式 `$E=mc^2$` 被正确渲染为数学公式
- [ ] 块级公式 `$$\sum_{i=1}^{n} i$$` 被正确渲染为独立公式块
- [ ] 同一字符串中混合行内和普通文本时正确渲染
- [ ] 嵌套对象中的 LaTeX 公式被正确渲染
- [ ] 数组元素中的 LaTeX 公式被正确渲染
- [ ] 无效的 LaTeX 语法优雅降级,显示原始文本而非崩溃

### 2.5 路径复制格式

- [ ] 选择"默认"格式后,双击节点复制的路径格式为 `data.users[0].name`
- [ ] 选择"点号"格式后,双击节点复制的路径格式为 `data.users.0.name`
- [ ] 选择"JSONPath"格式后,双击节点复制的路径格式为 `$.data.users[0].name`
- [ ] 选择"括号"格式后,双击节点复制的路径格式为 `["data"]["users"][0]["name"]`
- [ ] 选择"Python"格式后,双击节点复制的路径格式为 Python `.get()` 链式调用
- [ ] 自定义格式支持 `{key}` 占位符
- [ ] 自定义格式支持 `{index}` 占位符
- [ ] 路径格式选择在重启后保持

### 2.6 错误处理

- [ ] 输入无效 JSON 后点击格式化,显示错误提示
- [ ] 错误提示包含行号信息
- [ ] 错误提示包含列号信息
- [ ] 错误提示显示错误位置附近的代码上下文
- [ ] 错误提示中的特殊字符被正确转义(XSS 防护)
- [ ] 修正 JSON 后重新格式化,错误提示消失

### 2.7 剪贴板

- [ ] 点击"粘贴"按钮后剪贴板内容被粘贴到编辑器
- [ ] 双击树节点后路径被复制到剪贴板
- [ ] 复制的路径可在其他应用中正常粘贴使用

### 2.8 托盘菜单

- [ ] 关闭窗口后应用图标出现在系统托盘
- [ ] 右键点击托盘图标显示菜单
- [ ] 点击"显示"后主窗口恢复显示
- [ ] 点击"隐藏"后主窗口隐藏
- [ ] 双击托盘图标后主窗口恢复显示
- [ ] 托盘菜单"置顶"选项可切换窗口置顶状态
- [ ] 托盘菜单置顶状态与窗口内图钉按钮状态同步
- [ ] 托盘菜单"开机自启"选项可切换自启动状态
- [ ] 托盘菜单"禁用更新检查"选项可禁用自动更新
- [ ] 托盘菜单"检查更新"选项触发手动更新检查
- [ ] 托盘菜单"退出"选项完全退出应用(不留托盘图标)

### 2.9 自动更新

- [ ] 应用启动时自动检查更新(网络可用时)
- [ ] 有新版本时弹出更新提示对话框
- [ ] 更新对话框显示版本号和更新说明
- [ ] 点击"立即更新"开始下载新版本
- [ ] 下载完成后提示重启应用
- [ ] 点击"稍后更新"关闭对话框,不安装更新
- [ ] 禁用更新检查后,启动时不再弹出更新提示

### 2.10 开机自启

- [ ] 在托盘菜单中启用"开机自启"
- [ ] 重启系统后应用自动启动
- [ ] 在托盘菜单中禁用"开机自启"
- [ ] 重启系统后应用不再自动启动

---

## 3. 性能测试

- [ ] 格式化包含 1000+ 个键的 JSON 对象,响应时间在可接受范围内
- [ ] 格式化嵌套 50 层的 JSON,不出现崩溃或卡死
- [ ] 超过最大渲染深度的 JSON 显示深度限制提示,不崩溃
- [ ] 大型 JSON 的树形视图可流畅滚动
- [ ] 大型 JSON 树形视图的展开/折叠操作响应流畅

---

## 4. 边界情况

- [ ] 输入空 JSON 对象 `{}` 后格式化正常
- [ ] 输入空 JSON 数组 `[]` 后格式化正常
- [ ] JSON 中包含 Unicode 字符(中文、日文等)时正确显示
- [ ] JSON 中包含 Emoji 字符时正确显示
- [ ] JSON 中包含转义字符(`\n`, `\t`, `\\` 等)时正确处理
- [ ] JSON 字符串值包含超长文本时界面不溢出
- [ ] JSON 键名包含特殊字符时路径复制格式正确

---

## 5. 窗口操作

- [ ] 可拖拽标题栏移动窗口位置
- [ ] 可拖拽窗口边缘调整窗口大小
- [ ] 最小化后可从任务栏恢复窗口
- [ ] 最小化后可从托盘图标恢复窗口(macOS)
- [ ] 关闭窗口后应用最小化到托盘(不完全退出)
- [ ] 窗口置顶后始终显示在其他窗口之上
- [ ] 重启应用后窗口位置和大小保持上次状态(window-state 插件)
- [ ] 重启应用后置顶状态保持

---

## 6. 主题测试

- [ ] 深色主题下所有 UI 元素显示正常,无白色闪烁
- [ ] 浅色主题下所有 UI 元素显示正常
- [ ] 深色主题下文字可读性良好
- [ ] 浅色主题下文字可读性良好
- [ ] 主题切换后树形视图颜色正确更新
- [ ] 主题切换后错误提示颜色正确更新
- [ ] 重启应用后主题设置保持(localStorage 持久化)

---

## 7. 安全测试

- [ ] JSON 值包含 `<script>alert(1)</script>` 时不执行脚本
- [ ] JSON 值包含 `<img src=x onerror=alert(1)>` 时不执行脚本
- [ ] JSON 键名包含 HTML 标签时被正确转义显示
- [ ] 路径复制中包含特殊字符时被正确处理
- [ ] 错误提示中的 JSON 上下文片段被正确转义

---

## 8. 回归测试

以下为历史修复的 P0 问题,每次发布前必须验证:

- [ ] **TreeView XSS 修复** — JSON 值中的 HTML 标签不被执行,显示为转义文本
- [ ] **更新对话框 XSS 修复** — 更新说明中的 HTML 标签不被执行
- [ ] **错误提示 XSS 修复** — 错误上下文中的 HTML 标签不被执行
- [ ] **竞态条件修复** — 快速连续点击格式化按钮不导致界面错乱
- [ ] **深度限制修复** — 超深嵌套 JSON 触发深度限制提示而非崩溃
- [ ] **CSP 修复** — 应用内容安全策略正常生效,控制台无 CSP 违规警告
- [ ] **Rust 退出修复** — 从托盘菜单"退出"可完全退出应用,不残留进程
- [ ] **macOS 最小化修复** — macOS 上最小化后可从 Dock 或托盘正常恢复窗口

---

## 测试结果

| 项目 | 内容 |
|------|------|
| 测试日期 | |
| 测试人员 | |
| 测试平台 | |
| 应用版本 | |
| 通过项数 | |
| 失败项数 | |
| 跳过项数 | |

### 失败项记录

| 编号 | 测试项 | 失败现象 | 严重程度 | 备注 |
|------|--------|----------|----------|------|
| | | | | |

### 备注

